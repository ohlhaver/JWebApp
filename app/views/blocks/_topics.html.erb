<% if block.stories.any? %>
<div class='news_block'>
  <h2><%= link_to block.name, topic_path( block.id ) %></h2>
  <% block.stories.each do |story| %>
    <div class='headline'><%= render_story_preview( story ) %></div>
  <% end %>
</div>
<% end %>