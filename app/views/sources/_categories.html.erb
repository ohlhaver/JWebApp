<%= render_category_link( 'cluster.all.label', base_url, :all ) %>
<% categories.each do | category | %>
  <span class='separator'>-</span> <%= render_category_link( category.name, base_url, category.id ) %>
<% end %>